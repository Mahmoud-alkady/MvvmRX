# Uncomment the next line to define a global platform for your project
  # platform :ios, '12.0'

target 'TestRX' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for TestRX

  pod 'Alamofire', '~> 5.0.0-rc.3'
  pod 'Kingfisher', '~> 5.0'
  pod 'RxSwift', '~> 5'
  pod 'RxCocoa', '~> 5'

  target 'TestRXTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'TestRXUITests' do
    # Pods for testing
  end

end
